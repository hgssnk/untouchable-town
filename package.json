{
  "name": "untouchable-town",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "live-server --port=8000 --no-browser"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
